{% extends 'base.html.twig' %}

{% block body %}
<h1>Formulaire légal</h1>
<div class="form">
    <form>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Soumettre</button>
        {{ form_end(form) }}
    </form>
</div>
{% endblock %}